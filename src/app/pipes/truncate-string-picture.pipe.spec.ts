import { TruncateStringPicturePipe } from './truncate-string-picture.pipe';

describe('TruncateStringPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateStringPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
